import 'package:flutter/material.dart';

class ItemSpaceChatting extends StatelessWidget {
  const ItemSpaceChatting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Center(
        child: Text('This is space chatting'),
      ),
    );
  }
}
